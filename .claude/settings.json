{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Git Flow 브랜치 상태 확인 중...",
            "command": "branch=$(git branch --show-current 2>/dev/null); if [ \"$branch\" = \"main\" ] || [ \"$branch\" = \"develop\" ]; then printf '{\"systemMessage\": \"[Git Flow] ⚠️  보호 브랜치 경고: `%s` 브랜치입니다.\\n새 작업은 feature/fix/hotfix 브랜치를 먼저 생성하세요.\\n  feat  → git checkout -b feature/<name> develop\\n  fix   → git checkout -b fix/<name> develop\\n  hotfix→ git checkout -b hotfix/<name> main\"}\\n' \"$branch\"; else printf '{\"systemMessage\": \"[Git Flow] 현재 브랜치: `%s`\"}\\n' \"$branch\"; fi"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Git Flow 보호 브랜치 검사 중...",
            "command": "input=$(cat); cmd=$(echo \"$input\" | grep -o '\"command\":\"[^\"]*\"' | head -1 | sed 's/\"command\":\"//;s/\"$//'); branch=$(git branch --show-current 2>/dev/null); if echo \"$cmd\" | grep -qE '^git (commit|push|merge)' && { [ \"$branch\" = \"main\" ] || [ \"$branch\" = \"develop\" ]; }; then echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"block\",\"permissionDecisionReason\":\"Git Flow 위반: main/develop에 직접 commit/push/merge 금지. feature|fix|hotfix 브랜치에서 작업 후 PR로 병합하세요.\"}}'; fi"
          }
        ]
      }
    ]
  }
}
